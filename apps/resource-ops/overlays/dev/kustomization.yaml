apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: resource-ops-dev

resources:
  - namespace.yaml
  - ../../base

patches:
  - path: patch-replicas.yaml
  - path: patch-configmap.yaml
  - path: patch-external-secret.yaml

images:
  - name: team2-backend
    newTag: dev-latest
